import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/Mail/components/body.dart';

class CheckMailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(
        child: Column(),
      ),
    );
  }
}
